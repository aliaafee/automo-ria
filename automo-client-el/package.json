{
  "name": "automo-client-el",
  "version": "1.0.0",
  "description": "Automo Client Electrone",
  "productName": "Automo Client",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager .",
    "build-linux": "electron-packager . --platform=linux --out=build --asar --overwrite",
    "build-win": "electron-packager . --platform=win32 --out=build --asar --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ali Aafee",
  "license": "ISC",
  "dependencies": {
    "electron": "^6.0.11",
    "electron-packager": "^14.2.1",
    "query-string": "^6.12.1",
    "sqlite3": "^4.1.0"
  },
  "devDependencies": {
    "electron-rebuild": "^1.8.6"
  }
}
